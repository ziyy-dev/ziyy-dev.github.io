# `<br>`

The `<br>` element produces a line break in text (carriage-return).

## Example

```html
<p>This is the first line.<br />This is the second line.</p>
```

## Attributes

### `n`

The number of line breaks to produce. If not specified, it defaults to 1.
