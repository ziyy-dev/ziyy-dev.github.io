# `<c>`

The `<c>` element is used to style text with a specific color. It is often used to highlight important text or to make certain words stand out in a terminal output.

## Example

```html
<c red>This text is red</c>
```

## Attributes

This element only includes the [global attributes](../globals.mdx).
