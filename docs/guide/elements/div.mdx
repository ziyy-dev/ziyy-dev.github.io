# \<div>

The `<div>` element is used to create a block-level container for grouping content. It is often used to structure the layout of a terminal output or to apply styles to a section of text.

## Example

```ziyy
<div>This is a block-level container.</div>
```

## Attributes

This element includes the [global attributes](../globals.mdx).
