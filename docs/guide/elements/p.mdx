# \<p>

The `<p>` element is used to create a paragraph of text. It is often used to group related text together and provide structure to the content.

## Example

```ziyy
<p>This is a paragraph of text.</p>
```

## Attributes

This element includes the [global attributes](../globals.mdx).
