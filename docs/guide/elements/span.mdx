# `<span>`

The `<span>` element is used to group inline elements in a terminal. It does not inherently represent anything and is used primarily for styling purposes.

## Example

```html
<span>This is a span element.</span>
```

## Attributes

This element includes the [global attributes](../globals.mdx).
